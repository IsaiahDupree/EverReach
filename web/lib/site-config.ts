import type { SiteConfig } from '@seo-workspace/next-seo-kit';

export const siteConfig: SiteConfig = {
  siteUrl: 'https://www.everreach.app',
  siteName: 'EverReach',
  locale: 'en_US',
  twitterHandle: '@everreachapp',
  defaultOGImage: 'https://www.everreach.app/og-default.png',
};
